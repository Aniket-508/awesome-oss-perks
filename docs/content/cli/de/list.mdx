---
title: ossperks list
description: Alle verfügbaren OSS-Vorteilsprogramme auflisten, optional nach
  Kategorie gefiltert.
---

## Verwendung

```
ossperks list [options]
```

**Alias:** `ossperks ls`

## Optionen

| Flag                    | Beschreibung                                  |
| ----------------------- | --------------------------------------------- |
| `-c, --category <slug>` | Nach Kategorie-Slug filtern (z. B. `hosting`) |
| `--json`                | Rohes JSON ausgeben                           |
| `-h, --help`            | Hilfe anzeigen                                |

## Beispiele

```bash

# Alle Programme auflisten

ossperks list

# Nur Hosting-Programme filtern

ossperks list --category hosting

# Maschinenlesbare JSON-Ausgabe

ossperks list --json
```

## Ausgabe

```
OSS Perks — 48 Programme
────────────────────────────────

  1password              1Password for Open Source Projects
  algolia                Algolia for Open Source
  anthropic-claude       Anthropic Claude for Open Source
  ...
```

import { DynamicLink } from "fumadocs-core/dynamic-link";

Jede Zeile zeigt den Programm-Slug und den vollständigen Namen. Verwenden Sie <DynamicLink href="/[lang]/cli/show">`ossperks show <slug>`</DynamicLink>, um vollständige Details anzuzeigen.
