---
title: ossperks open
description: 在默认浏览器中打开 OSSPerks 网站。
---

## 用法

```
ossperks open
```

## 说明

在默认浏览器中打开 [ossperks.com](https://www.ossperks.com)。根据平台使用相应的命令（macOS 上为 `open`，Linux 上为 `xdg-open`，Windows 上为 `start`）。

如果无法启动浏览器，则会将 URL 打印到终端。

## 示例

```bash

# 打开网站

ossperks open
```
